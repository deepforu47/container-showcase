apiVersion: v1
kind: Service
metadata:
  name: flask-basics-service
spec:
  selector:
    app: flask-basics
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30081
  type: NodePort
